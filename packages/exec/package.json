{
  "name": "@hydra/exec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit --project ../../tsconfig.base.json",
    "typecheck": "tsc --noEmit --project ../../tsconfig.base.json"
  },
  "dependencies": {
    "@hydra/core": "workspace:*",
    "@hydra/risk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
